<main>
    <h1> {{title}}</h1>
    <br>
    <h2>
        {{search}}
    </h2>
    <ul>
        {{#each showData}}
        <li>
            <a  href="shows/{{show.id}}" class="show-link">{{show.name}}</a>
        </li>
        {{/each}}
    </ul>
    <a href="/">Make another search</a>
</main>